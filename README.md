# Checklists

A SaaS app for building, scheduling and tracking operational checklists.

**New here? Read [SETUP.md](SETUP.md) first** — it walks through creating the
Supabase project and getting the app running. [DEPLOY.md](DEPLOY.md) takes it
from there: GitHub, Vercel, your own domain, and email.

---

## Hierarchy

```
Board (a company or department)
└── Checklist (a reusable template)
    └── Group (a section)
        └── Item  (up to 5 levels of sub-items)
```

A parent item completes automatically once all of its children are complete.

## Layout

```
apps/
  web/                  Next.js 16 app — the product, at app.gidlist.com
  site/                 Next.js 16 app — the marketing site, at gidlist.com
packages/
  core/                 Shared domain logic. No React, no Next, no React Native,
                        so the Expo app (Phase 8) can import the same rules.
  design/               Design tokens — colour, radius, elevation, type slots.
                        Imported by both apps so neither can drift from the
                        brandbook.
supabase/
  migrations/           Database schema, applied in order
```

Two apps, two Vercel projects, one repository. They are separate deployments on
purpose: an edit to the marketing site must not be able to break the product,
and the product's build must not gate a copy change. What they share, they share
through `packages/` rather than by copying — which is why the palette moved out
of `apps/web/src/app/globals.css` and into `packages/design/tokens.css`.

The site runs on port **3001** so both can run at once.

## Commands

Run from the repository root. **On Windows, add `.cmd` to every Node command** —
`pnpm.cmd`, `npx.cmd`, `npm.cmd`. PowerShell's default execution policy blocks
the script-based shims that npm installs, and `.cmd` sidesteps it without
changing any machine-wide security setting.

| Command          | What it does                                  |
| ---------------- | --------------------------------------------- |
| `pnpm dev`       | Start both: product :3000, site :3001         |
| `pnpm build`     | Production build — run before deploying       |
| `pnpm typecheck` | Check types across every package              |
| `pnpm lint`      | Lint                                          |
| `pnpm db:types`  | Regenerate DB types after a schema change     |

## Decisions worth knowing

**Security lives in the database, not the app.** Every table has Row Level
Security policies. A bug in a page, a forged request, or a leaked public key
still cannot read another company's data, because Postgres itself refuses. The
UI is a convenience layer on top of that, never the control.

**Checklist templates are versioned** (from Phase 2). Editing a template in
March must not change what a January submission looked like, or the compliance
history becomes false — and that is not fixable retroactively.

**Scheduled occurrences are materialised ahead of time** (from Phase 3), by a
nightly job that also flips overdue drafts to "Missed". Computing them on the
fly would make the submissions dashboard slow and hard to filter.

**Payments are web-only, by necessity.** Apple and Google require their own
in-app purchase system — and take 15–30% — for subscriptions sold inside an
app. The mobile app therefore has no purchase screen at all: people subscribe
on the web and the app just signs them in. This is the standard B2B approach
and is explicitly permitted.

**"Space" in the interface is `board` in the database.** The product calls these
spaces; the tables are still `boards` and `board_members`. Renaming them would
mean rewriting tables, policies and security functions for something no user
ever sees. The divergence is deliberate.

**Dates never go through `toISOString()`.** It converts to UTC, so in Tashkent
(UTC+5) local midnight becomes 19:00 the previous day — which silently made
"next day" return the same date and the server's "today" return yesterday
before 05:00. Use the helpers in `packages/core/src/dates.ts`, and resolve
"today" through `lib/timezone/server.ts`, which uses the viewer's own timezone.

**Weekday and month names come from `Intl`, not the message catalogue.** Every
locale already knows them, and hard-coding them would need redoing each time an
administrator adds a language in the app.

**Roles split governance from content.** `owner` and `admin` manage people and
space branding; `editor` builds and schedules checklists; `member` fills them
in. Two database functions carry the distinction — `is_board_admin` for
governance, `is_board_editor` for content — kept separate on purpose, because
one function answering two questions is how permissions quietly drift. In app
code use `canGovern()` and `canEditContent()` from `packages/core`, never a
hand-written role comparison.

**The app's address is configuration, not code.** It appears in exactly one
place — `NEXT_PUBLIC_APP_URL` — plus three external settings: the Vercel custom
domain, Supabase's redirect URLs, and the verified sending domain in Resend.
The live address is `app.gidlist.com`; moving it anywhere else is those four
settings and nothing else.

Do **not** host the app on a subfolder (`gidlist.com/app/`). That needs a
reverse proxy plus a Next.js `basePath`, which puts a permanent failure point
between users and the app — and buys nothing, since everything behind the login
is unindexed anyway. The subfolder-for-SEO reasoning applies to marketing pages,
not to the application.

**Email is a notification, never the record.** An invitation lives in the
database and works whether or not a message arrives, so sending is best-effort
and can never fail an invitation. `lib/email/send.ts` is a thin wrapper over the
provider's HTTP API rather than their SDK — swapping provider means editing one
file. It degrades to a no-op when unconfigured, and the UI says which happened.

**App-wide settings are cached across requests; anything personal is not.** The
language list and the translation overrides are read on every page render, and
they change a few times a month — so they go through `unstable_cache` with an
hour's life and a shared tag, cutting two sequential database round trips from
every request in the app. They are read with `createPublicClient()`, a Supabase
client with no session, because a cached function may not touch cookies.

The rule that makes this safe is narrow: **only data whose SELECT policy is
`to public using (true)` may be cached this way.** Those rows are identical for
every visitor, so there is nothing personal to leak between them. Cache anything
user-scoped like this and Row Level Security has no one to identify — one
person's rows would be stored and served to the next. When in doubt, don't.

Writes call `updateTag`, not `revalidateTag`: the latter now serves stale
content while it refetches, so an administrator would save a translation and
still be looking at the old one.

**Ask for the user through `getUser()`, never `supabase.auth.getUser()`.**
That call verifies the token against Supabase's auth server — a network round
trip, not a local decode. Rendering one page used to make it three to five
times over. The helper in `lib/supabase/server.ts` is memoised with React's
`cache`; calling the client directly bypasses the memo and quietly restores the
round trip. `proxy.ts` is the one exception: it runs before rendering, in its
own invocation, and its call is what refreshes the session cookie.

**`database.types.ts` is hand-written, and `pnpm db:types` must not overwrite
it.** This schema has no Postgres enums — roles and statuses are `text` with
CHECK constraints — so the generator can only produce `string` where this file
has `BoardRole` and `SubmissionStatus`. That narrowing is what makes
`canGovern()` and the status filters typecheck. The script therefore writes
`database.generated.ts`, which is a reference to diff against. Converting the
constraints to real enums would retire the arrangement; that is open item 12.

**Platform access is capabilities, not a role, and not space ownership.** A
space owner runs their own company's data; platform capabilities reach across
every customer. They are separate systems and must stay so — interface wording
is shared by all customers, so no owner can be allowed to grant it.

Capabilities rather than a role because a role is a bundle, and a bundle is
always slightly wrong for somebody: you end up with "translator plus billing",
then "translator plus billing minus refunds". `translations`, `accounts` and
`grants` compose, and a new one is a row in `platform_capabilities`.

**The root stays out of the app.** `set_platform_grant` refuses to hand out
`grants` itself, so a master can delegate the others but cannot mint another
master, and nobody can promote themselves. That property is why the old boolean
was SQL-only, and it is the one worth keeping while everything below it becomes
delegable. Ask with `has_platform_capability()` — it is `stable` and
`security definer`, so it works inside an RLS policy.

**The Content Security Policy is built per request in `proxy.ts`**, not in
`next.config.ts`, because a strict `script-src` needs a fresh nonce and a static
config cannot produce one. Next reads the nonce back out of the request headers
and stamps it on its own scripts; `strict-dynamic` then refuses anything else.
Every response path in the proxy must set the header — one that escapes without
it has no policy at all, and the gap would be invisible.

## Open items

Agreed but not built. Kept here rather than in a chat so nothing depends on
remembering a conversation.

| # | Item | Note |
| - | ---- | ---- |
| 1 | ~~**Deployment**~~ | **Done.** Live at `app.gidlist.com` — Vercel project `gidlist-web`, functions pinned to `fra1` to match the database, invitation email sending from `noreply@gidlist.com` via Resend. [DEPLOY.md](DEPLOY.md) records the steps and the four places the address is configured. Remaining: the app has still never been used on a phone, despite being built mobile-first, and bare `gidlist.com` still shows the registrar's parking page with no valid certificate. |
| 1a | ~~**Split development from production**~~ | **Done.** `gidlist-dev` (`zffoidgzuyhojnydshkq`, eu-central-1) serves `pnpm dev`; `checklists` (`ivqprkzqnoiffqlbfkkd`) serves `app.gidlist.com` and holds real records. The CLI is linked to **dev**, so `supabase db push` and `pnpm db:types` both act on development — production is still changed by hand in its SQL Editor. **Order: development first, production second**; see SETUP.md. This also unblocks running `supabase/tests/security.sql` in CI, which previously had no database to reach. |
| 2 | ~~**Supabase auth email (SMTP)**~~ | **Done.** Sign-up and password-reset mail goes through Resend's SMTP (`smtp.resend.com`, username the literal word `resend`) from `noreply@gidlist.com`, with the auth rate limits raised off the defaults that protect Supabase's shared sender. The built-in sender allowed a few messages an hour and failed by going quiet — somebody would sign up, see "check your email", and leave. Note the real ceiling is now Resend's plan, not Supabase's: the free tier is 100/day shared with invitation mail, so onboarding a shift of 50 costs the day's allowance. |
| 2c | **Nothing is served statically** | Measured from Tashkent against production, best of 8: a CDN asset 190ms, the proxy redirect 231ms, `/` 452ms, `/login` 463ms. So ~190ms is network, ~41ms the proxy, **~221ms server rendering, and 11ms all database work**. Every page is rendered on demand because `proxy.ts` and the Supabase client read cookies on every request, so even the login form costs a function invocation. This is the largest remaining lever and it is what item 2b would fix. Note the network leg is inflated by requests entering Vercel at its **Hong Kong** edge before reaching Frankfurt. |
| 2a | ~~**Remaining per-request round trips**~~ | **Done, 3 Sep 2026 — and the measurement is the point of this row now.** Two real fixes. **(1) The proxy stopped calling the auth server.** `auth.getUser()` ran on every non-static request and made a network call to Dublin before any page code began. It is now `getClaims()`, which verifies the token's signature in process with WebCrypto — both databases already publish **ES256** keys at `/.well-known/jwks.json`, so this was available and unused, and auth-js caches the public key in a **module-level** `GLOBAL_JWKS` keyed by storage key, which is what makes it work despite the proxy building a fresh client per request. It is called with **no argument** deliberately: that routes through `getSession()` internally, which is what refreshes an expiring token, and passing the token by hand would sign everyone out hourly. **This is not the `getSession()` the old comment warned against** — that warning was correct, `getSession()` trusts a client-controlled cookie; `getClaims()` checks the signature against Supabase's own key. What it gives up, stated plainly: a signature check cannot know an account was deleted a minute ago, so such a token satisfies the proxy until it expires. Acceptable because this gate is not the control — the authoritative `getUser()` still runs once per render beside the data, and RLS decides every row underneath both. **(2) One `profiles` row was read three times in one render.** On `/dashboard/account` the layout wanted `full_name`/`avatar_url`, the page wanted the same two again, and `getLocale()` wanted `locale` whenever no language cookie was set — three round trips for one row of three columns. Now `lib/account/profile.ts`, memoised with React `cache` and **not** `unstable_cache`, because the row is keyed on `auth.uid()` and must never be shared between users. **What is left is not worth doing, and 2c says why:** of a 452ms page from Tashkent, database work is **11ms**. `my_platform_capabilities()` on each dashboard render is one of those milliseconds. Chasing further round trips optimises 2% of the page. |
| 2b | **Migrate to Cache Components** — *blocked, see 2d* | Investigated and deliberately not attempted. The migration guide is explicit: when a cookie drives an attribute on `<html>` — `lang`, `data-theme` — reading it on the server makes the whole subtree request-bound, so there is no child left to wrap in `<Suspense>`. Theme is solvable with a pre-paint script. **Locale is not**: a static shell is one shell per route, and with three languages selected by cookie rather than URL there is no single correct version of any translated text. The shell would hold layout chrome and nothing else, so the 221ms of rendering does not disappear — it moves behind a streamed boundary. That buys first byte at ~190ms instead of 452ms, which is perceived speed, not total, in exchange for a refactor across 22 pages and 6 layouts in the area most likely to produce a visible regression. Revisit only after 2d. |
| 2d | **Locale in the URL** — *reassessed 3 Sep 2026; see 2e before doing this* | `/uz/...`, `/ru/...` instead of a cookie. Makes a language shareable by link, and is the prerequisite that would make 2b worth doing. **The SEO argument in the original entry does not apply**: every page of the product is behind a login and sends `private, no-store`, so nothing here is indexed — that benefit belongs to `apps/site`, which already has locale in the URL. **And the round-trip argument is gone**: this used to remove the `profiles.locale` read, but 2a removed it instead by sharing the row. So what remains is one product feature — a shareable language link — plus unblocking a change (2b) that the 2b row itself judges to be perceived speed rather than total. Cost is 26 pages and 6 layouts moved under `[locale]`, every internal link and every server-action `redirect()` rewritten, and rework of `proxy.ts`, which is the security boundary. Worth doing for the product feature if that feature is wanted; not worth doing as a performance change, which is what it was queued as. |
| 2e | **Requests enter Vercel at Hong Kong** — *the largest remaining lever, and it is not code* | `X-Vercel-Id` on both hosts reads `hkg1::fra1::…`: a request from Central Asia reaches Vercel's **Hong Kong** edge, is forwarded to the function in **Frankfurt**, which queries **Dublin**, and the answer returns the same way. **Measured, with a control case.** A static CDN asset — no proxy, no rendering, no database — came back in ~418ms TTFB, while `/login` came back in ~700ms. So ~60% of the wait is network and TLS before the application does anything, and 2c's Tashkent figures agree in shape: 190ms for a CDN asset against 452ms for a page. Item 41 already established that moving the *function* does not help, because `fra1` beat `dub1` from Andijan by 59ms; this is about where the request **enters**, which no code change reaches. **`app.gidlist.com` is DNS-only on Cloudflare** — there is no `cf-ray` on any response, so nothing is proxied and Vercel's anycast picks the entry edge. Putting the record behind Cloudflare's proxy would terminate TLS at Cloudflare's nearest presence, Tashkent among them, and carry the rest over their backbone — attacking the two-round-trip handshake that the Hong Kong path is charging for. **Tested, and the answer is no — do not do this.** Two things were wrong with the plan above. First, `gidlist.com` is not on Cloudflare at all: its nameservers are GoDaddy's (`domaincontrol.com`), and it was `unumis.com` that is on Cloudflare — so this was never a DNS toggle, it was moving an entire zone of eight records, five of them the Resend email records that make `noreply@gidlist.com` work. A mis-imported DKIM record does not fail loudly; mail keeps sending and starts landing in spam. Second, and decisive: **the premise was tested for free against `unumis.com`, which is already proxied**, and Cloudflare serves this network from **Warsaw** (`CF-RAY: …-WAW`), not Tashkent. Handshake to Cloudflare 115ms TCP / 250ms TLS, against Vercel's Hong Kong path at 127ms / 276ms — about **25ms**, which is noise. So the trade was: move every DNS record for the product and its email, in exchange for 25ms. Abandoned. **The lesson worth keeping is the method, not the result:** the hypothesis named a city, the city was checkable against a domain already configured that way, and checking cost one command. It should have been the first step rather than the fourth. **What this leaves.** Of a ~700ms page on this network: ~280ms is TLS and TCP setup, which is distance to any major CDN and not addressable; ~140ms is request and response transit; ~280ms is the application. Only the last is ours, 2a has taken the round trips out of it, and what remains is render time and cold starts — measured in 2f, and it is cold starts. |
| 2f | **Cold starts cost more than everything else measured put together** — *measured 3 Sep 2026* | Server think time, isolated as `time_starttransfer − time_appconnect` so no network is in the figure. After leaving the function idle for 17 minutes: **1570ms**, then 487ms, then 700ms as it warmed. Steady warm state is **405–430ms**, with occasional 650ms spikes. So a **cold start costs about 1150ms on top of everything else**, and the honest read of a first visit is ~1900ms of server time, not the ~420ms a warm measurement suggests. **This matters more here than it would elsewhere, because of how little traffic the product has.** With a handful of accounts, the function is idle most of the time, so the *typical* visit is a cold one — the warm figures everything above was optimised against describe the rarer case. It also explains 2c's original ~221ms estimate being so much lower: repeated measurement keeps the function warm and measures the state real users least often get. **What to try, cheapest first.** (a) Confirm **Fluid Compute** is enabled on the Vercel project — it keeps instances alive between invocations and is a toggle, not a code change. (b) A keep-warm ping is the obvious next thought and does not work on Hobby: two cron jobs maximum, both already used, and "once a day" scheduling would not keep anything warm anyway. (c) Only then look at what the function loads at boot. **Do not chase render time before this is settled** — 420ms of warm rendering is worth attacking only once the 1150ms in front of it is gone, and 2b would defer the 420ms while doing nothing at all about the 1150ms. |
| 3 | ~~**Audit log**~~ | **Done.** `audit_log`, written by triggers on `board_members`, `boards`, `platform_grants` and `subscriptions` — an action the app records is one the app can forget to record, and a trigger cannot be bypassed by a new code path, a direct API call, or a fix applied in the SQL editor. Governance only: a submission already records who ticked what, and auditing every checkbox would bury the twenty rows a year that matter. Append-only by construction — a read policy and nothing else, so Postgres refuses every write from every API role. Space entries are readable by that space's admins, platform entries by `grants` holders. No foreign keys, deliberately: they would erase the subject exactly when it became interesting, and would make deleting a space fail because it tried to record itself. |
| 4 | ~~**Void a submission, with a reason**~~ | **Done.** The last action in the app that destroyed compliance evidence. Voiding **annotates rather than overwrites**: status still says the record was missed, and three columns beside it say who decided it should not count and why. Making 'void' a status would have erased the very fact the reason explains. The reason is required — a void without one is a deletion that leaves a row behind. Admin only, checked inside set_submission_void, which is the only thing permitted to write those columns. Voided records leave the done/missed figures and both sides of the trend percentage, and are reported on their own: counting one as missed defeats the point, counting it as done is a lie. Audited on both void and lift. |
| 5 | ~~**Checklist preview**~~ | **Done.** On the Details tab, showing the checklist as its filler will read it. Previews the draft when there is one — that is the version about to be committed to — and the published one otherwise, so it is never empty on a checklist in use. Originally a separate presentational component, on the reasoning that the real fill sheet is bound to a submission id and reusing it would mean inventing a submission. **Superseded by 18**, which found that reasoning wrong: `readOnly` plus `answer: null` closes every write path without a submission existing at all. |
| 6 | **Per-checklist discussion** | Message thread per checklist, with Supabase realtime. |
| 7 | ~~**Login page redesign**~~ | **Done.** The sign-in page said nothing about what the product is. Now a two-column layout: the form first in reading order and on phones, the explanation beside it on a wide screen where an evaluator is and a worker is not — moved below rather than hidden on small screens, since a prospect opening the link on a phone would otherwise learn nothing. One copy of the words, reordered with CSS. | Currently a plain centred card; first thing a prospective customer sees. |
| 8 | ~~**Banner cropping**~~ | **Done, 30 Aug 2026.** A non-3:1 image was silently centre-cropped, which put the subject of any tall photograph off-screen. There is now a **Fill / Whole image** toggle and, under Fill, a **draggable focal point** — drag on the banner itself, or use arrow keys (Shift for a bigger step), so it is not a mouse-only control. The preview is the real banner at the real aspect ratio, because nobody can judge a crop from a description of one. **Framing is stored in the URL fragment**, not in new columns: `…/1.jpg#gidlist-framing=cover,20,80`. Three columns on each of `boards` and `checklists` would have been tidier, and worse in the way that matters — the framing describes *one specific image*, and a fragment cannot outlive that image being replaced, whereas a column would keep a focal point tuned for the old photograph and silently apply it to the new one. A bug nobody reports, because the banner merely looks slightly wrong. It also avoided a migration, which on this project means two hand-applied SQL runs and a chance for production to lag again. **The honest cost:** it cannot be queried in SQL and anyone reading the column sees a URL with something odd appended. Acceptable for a presentational hint; it would not be for anything a report reads. Nothing parses `banner_url` back into a storage path, and browsers never send a fragment when fetching an image, so appending one is inert. `setBannerFraming` reads the row rather than accepting a URL from the client — taking one would have reopened exactly the hole `saveMedia` exists to close, letting a caller repoint a banner at any address under the guise of adjusting a crop. Backwards compatible by construction: a bare URL parses to the old centre-crop defaults, and saving the default writes a bare URL rather than a second spelling of the same thing. Verified by executing `packages/core/src/appearance.ts` directly against 14 cases — round trips, replacement rather than appending, clamping, and hand-edited rubbish degrading to a default instead of throwing. |
| 9 | **Cross-section drag** — *sections done, nesting still not draggable* | An item can now be dragged into another section, including an empty one. **The cause was structural:** every sibling list built its own `DndContext`, so each list was a separate drag world with no drop targets outside itself — and dnd-kit does not nest contexts usefully. There is now one context for the whole builder, and drags route by a `type` on the sortable rather than by guessing from the id. **Moving an item moves its subtree.** `group_id` is not nullable and decides where a row renders, so a child left behind would render detached or not at all. The server clears `parent_item_id` on the moved item, then touches each descendant in breadth-first order assigning its own unchanged `parent_item_id` — which looks like a no-op and is not: `update of parent_item_id` fires on assignment, not on change, so `set_checklist_item_depth` re-runs and copies down the new depth and section from the already-updated parent. Depth is never written from the client; note top level is depth **1**, not 0. **Deliberately out of scope:** dropping *onto* an item to nest it. That gesture means "make this a sub-item of that", which needs drop targets meaning "inside" rather than "beside" and a `MAX_ITEM_DEPTH` check across the whole subtree. Dropping on a nested item is ignored rather than guessed at; sub-items are still made with the Add sub-item control. The tree surgery lives in `lib/checklists/tree.ts`, pure and import-free so it can be executed on its own — drag-and-drop inside an authenticated page is the hardest thing in this codebase to check by hand, and separating the part that can be wrong from the part that needs a mouse is what makes it checkable at all. |
| 10 | ~~**Browser tab titles**~~ | **Done.** Thirteen customer-facing pages moved from `export const metadata` to `generateMetadata`, which can await `getTranslations()` — static metadata is evaluated without a request, so it has no way to know which locale the cookie asked for. The five admin pages stay English, matching the rest of that area, which has one reader. |
| 12 | **Real Postgres enums** | Roles, member statuses, version statuses, schedule kinds and submission statuses are `text` + CHECK. Converting them to enum types would let `pnpm db:types` generate `database.types.ts` correctly instead of it being hand-maintained, removing a standing chance of someone widening every union back to `string` by running one command. Needs a migration per type and a types regeneration. |
| 13 | ~~**Enforce the CSP**~~ | **Done.** Built per request in `proxy.ts`, because a strict `script-src` needs a fresh nonce and a static config cannot make one. `strict-dynamic` with a nonce means an injected `<script src>` never runs. Unblocked by the 2b decision: the nonce forces dynamic rendering, which no longer conflicts with anything now that Cache Components is deferred. Two deliberate loosenings, both documented in the file: `style-src` keeps `'unsafe-inline'` because seven components set `style={{…}}` and CSP ignores `'unsafe-inline'` once a nonce is present, so adding one would break the builder rather than tighten it; `'unsafe-eval'` is development-only, for React's dev overlay. Verified in a browser: nonce unique per request, 22 of 24 scripts nonced, React hydrated, no violations. |
| 13a | ~~**Verify the CSP in production**~~ | **Done, and it found something.** Development allows `'unsafe-eval'`, so the local check could not have caught it: production reported a blocked eval from Zod probing for JIT support with `Function("")`. Benign — Zod catches it and interprets schemas instead — but a console with a standing violation is one nobody reads, so the next real one would arrive unnoticed. Fixed with Zod's own `jitless` option rather than by adding `'unsafe-eval'`, which would have weakened the policy for every script to quiet one library. **The fix took two attempts**: Zod decides about JIT when a schema is *constructed*, so configuring it inside a module is not enough — the bundler picks chunk order and a schema elsewhere can be built first. `instrumentation-client.ts` is the one place Next guarantees runs before the app becomes interactive. |
| 14 | ~~**Rate limiting**~~ | **Done.** `enforce_rate_limit()` with a rolling window, enforced in the database — a limit in a server action is bypassed by calling PostgREST directly, which is easier than driving the interface and therefore what an abusive client does. Invitations 30/hour and 100/day per person, materialisation 60/hour. Only *pending* invitations count: accepted members are already bounded by the plan, and it is invitations that never accept which cost sending reputation. Refusals carry SQLSTATE `PT429`, which PostgREST answers as HTTP 429, so a client sees the standard signal rather than a 500. |
| 15 | ~~**Error tracking**~~ | **Done.** `src/instrumentation.ts` exports Next's `onRequestError`, so every server failure — Server Component render, Server Action, route handler, proxy — is caught by the framework rather than by remembering to try/catch. No SDK: it is a framework export and one fetch, so it works on this version of Next today instead of whenever a vendor catches up. Always writes one structured JSON line tagged `app-error`; also posts to `ERROR_WEBHOOK_URL` when set, deduplicated to one notification per distinct error per ten minutes. **Headers are never forwarded** — `request.headers` carries the Supabase session cookie, and the payload is built from an allow-list rather than by stripping known-bad fields. Verified by throwing from a temporary route and reading the log line back. |
| 15a | ~~**Alerting on the nightly job**~~ | **Done.** An hourly `check-job-health` reads pg_cron's own `job_run_details` — rather than a record the jobs keep themselves, which would go quiet exactly when they do — and writes `system.job_stale` into the audit log, with a 12-hour cooldown so a lasting outage does not bury the history. Tolerances live in `job_expectations` as rows, so adding a job is an insert. A banner appears at the top of the admin area when anything is behind, and renders nothing otherwise: a permanent green tick stops being read within a week, and then the day it turns red it is not read either. **Push notification is still not wired** — the alert lands where somebody looks rather than reaching them. Doing that from Postgres needs `pg_net`; see 15b. |
| 15b | ~~**Push the job-health alert**~~ | **Done, pending configuration.** `check_job_health()` had been correct since it was written and had never told anybody anything: it wrote `system.job_stale` into the audit log, which only helps someone already looking, and the reason a job needs watching is that nobody is looking. It now also posts to the error webhook via `pg_net`, with the URL held in Vault rather than a table — anything holding it can post to the operator's notification channel, and Vault keeps it out of `pg_dump`. `notify_ops()` cannot fail loudly: a missing extension, missing secret, or refused request leaves the audit row intact and simply does not notify, because a check that aborts when its notifier is misconfigured stops recording staleness at all. The cost is that a failed push is itself invisible. **Two gaps found while building it, both worse than the original problem.** `expire-evidence` was never registered in `job_expectations`, so the watcher did not know it existed — and an empty alert list reads as good news. And the storage drain runs on Vercel, outside `cron.job_run_details`, so it could not be judged at all; it now calls `record_job_heartbeat()` on a successful run, with execute revoked from every ordinary role since forging a heartbeat silences the alarm. A self-reported heartbeat is what the original file argued against, and that objection stands: this catches a job that stops, loses its secret, or starts failing — all of which go quiet — but not one that runs, reports success, and does nothing useful. **Needs from the operator:** `pg_net` enabled and an `error_webhook_url` secret in Vault, on each database. Until then the audit row still lands and nothing else changes. |
| 16 | ~~**RLS test suite**~~ | **Done.** `supabase/tests/security.sql` — paste into the SQL Editor and run. 17 checks over tenant isolation, platform capabilities and plan limits, wrapped in a transaction that rolls back, so it creates and destroys its own fixtures and never touches real data. Five are positive controls: a "cannot see" assertion passes just as well when the fixture failed to build, so each negative is paired with the same query run by someone who *should* see rows. **Run it after every migration that touches a policy.** Not yet in CI — that needs a database CI can reach, which is open item 1a. |
| 17 | ~~**`submissions` retention**~~ — *closed 30 Aug 2026; partitioning deliberately not done* | **The file half.** Rows are cheap and files are not: a submission row is a couple of hundred bytes, a phone photograph is three megabytes. **The record is never deleted** — that is the product. What expires is the file: `photo_expired_at` / `file_expired_at` keep the fact that evidence existed, retention is `plans.evidence_retention_days` (90 / 365 / 730 / indefinite), `expire_evidence()` runs at 02:30 UTC and queues paths, and `GET /api/cron/storage-cleanup` drains the queue at 03:00 because removing a `storage.objects` row does not remove the file and SQL cannot call the Storage API. **The row half, which is what this row was left open for, and the answer is: do not partition.** The reasoning is worth keeping because the instinct is to partition and the instinct is wrong here. The compliance functions (`compliance_counts`, `compliance_trend`, `compliance_assignees`) already take `p_from`/`p_to` and filter `s.due_date between` them, board-scoped, on `submissions_checklist_due_idx` — so **query latency does not grow with history**, which is the usual reason to partition. Against that, partitioning costs real safety: every query must carry the partition key or it touches every partition, RLS and foreign keys both get more awkward across a partitioned parent, and somebody has to run partition maintenance forever. Paying that to solve a problem the query plans do not have would be the wrong trade. **What was actually wrong was the nightly job, and it was a real defect.** `expire_evidence()` opened by selecting *every* submission item holding a photo or file, joining it up through submissions, checklists, boards, subscriptions and plans, and only then comparing dates. There was no index to support that — `submission_items` carried only `(submission_id)` — so it sequentially scanned a table that is overwhelmingly rows with no attachment at all. Worse, an account on indefinite retention has `evidence_retention_days = null`, so **its rows can never expire and were re-gathered and re-joined every night forever** before being discarded at the end. Unbounded nightly work, invisible until it is not. **Fixed in `20260830100000_expire_evidence_scan.sql`:** a partial index on `updated_at where photo_path is not null or file_path is not null` — partial because attachments are the minority and an expired row leaves the index by itself when its paths are nulled — and the function rewritten to resolve each board’s retention window first, excluding indefinite-retention boards once instead of carrying them through the whole query. Boards number in the hundreds; submission items in the millions. Every step of the new join path is indexed. **Revisit partitioning only when a measurement says so**, not on instinct: if `submissions` passes roughly 50 million rows, or a compliance query over a one-month window stops returning in under a second, the trade changes. Until then the record simply accumulates, which is what a record is for. |
| 11 | **Member hierarchy within a space** | Reporting lines between members, so a supervisor can see and act on their own team rather than the whole space. Needed for planned functionality. Note this cuts across the current visibility model, which is flat: today a member sees only themselves and an editor sees everything. A hierarchy introduces a third case — "mine and my reports'" — which every submission and compliance policy would need to express. Design it before building it. |
| 18 | ~~**Full visual checklist preview**~~ | **Done.** The Details tab now renders the genuine `FillSheet` in `readOnly` mode, under the checklist's own banner and avatar, so an editor sees the sheet a filler sees. No throwaway submission was needed after all: every item is given `answer: null`, which makes `answerId` undefined, which makes `interactive` false independently of `readOnly` — so the checkboxes render `disabled` and both write paths return before reaching a server action. The submit form, the only reader of `submissionId`, is inside `{!readOnly ? … : null}` and never reaches the DOM. Nothing is written and no row enters the compliance record. This retires the second presentational component from item 5 and with it the drift between the two renderers. |
| 19 | ~~**Brandbook**~~ | **Done.** Published at [claude.ai/code/artifact/026f2f16](https://claude.ai/code/artifact/026f2f16-469e-4a20-89de-17251bcef19a) — colour roles, type scale, the mark, voice, and the three taglines. Drawn from `apps/web/src/app/globals.css`, which stays the source of truth: when the tokens change, the book changes. **Three decisions settled** — (a) the name stays Latin in all three languages, no `Гидлист`; (b) the audience is operations **and** offices, two rooms with one problem, which is why the voice rule *"name one room, not both"* exists; (c) each language gets its own tagline rather than a translation — `Get it done.` / `Hammasi bajariladi!` / `Всё будет выполнено!` — the English an instruction, the other two a promise, each built on the word its own language already uses for a finished record (`done` / `Bajarilgan` / `Выполнено`). The two exclamation marks are the sole, deliberate exception to the voice rule against them; the rule itself records the exception rather than being silently contradicted. |
| 20 | **Keep the handover documents current** | Recurring, not one-off. README, SETUP.md, DEPLOY.md and the assistant's memory file are what survive a context window. Whenever a decision is made or reversed, it goes in the README the same day; whenever the setup changes, SETUP.md changes with it. A stale handover is worse than none, because it is believed. |
| 21 | **Marketing website** — *A, B and C done, live* | **gidlist.com is live** in all three languages from the `gidlist-site` Vercel project (`apps/site`). DNS: apex `A @ 216.198.79.1`, `CNAME www 4e153814e3021330.vercel-dns-017.com`, TTL 600; the `app` CNAME and the five Resend records were left untouched and verified. **Phase A:** the app, shared tokens, locale in the URL with `Accept-Language` negotiation and hreflang, security headers. **Phase B:** hero, problem, what-it-does, how-it-works, pricing, closing CTA; scroll reveals are pure CSS `animation-timeline: view()` so a failed bundle cannot blank the page. **Phase C (done):** copy is editable at **Admin → Marketing site**, behind a new `site` platform capability kept separate from `translations` because a bad headline is public and a bad label is not. `site_content` is the same overrides-on-top-of-the-bundle shape as `translations`, so an empty table renders a complete page and **deleting a row is the undo**. Verified against an unreachable host: the site still rendered every section in all three languages. The catalogue moved to `packages/core/src/site-messages.ts` because the admin editor must show the string it overrides — two copies would drift. **Edits appear within ~5 minutes** (ISR); the editor says so, because otherwise it looks broken. **Still hand-mirrored:** pricing figures in `apps/site/src/lib/pricing.ts` drift silently if a price changes in SQL. **Phase D:** blog, knowledge base, sitemap, structured data. **CSP done:** built per request in `apps/site/src/proxy.ts` with a fresh nonce, and stricter than the product’s — `connect-src` is `'self'` alone, because the site’s only database read is `server-only` and its browser never contacts Supabase at all. Set on every response path including the locale redirect; verified that the nonce differs per request and that all 17 of the page’s scripts carry it. **Motion: done** — see 40. WebGL deliberately skipped, for weight. `www.gidlist.com` 308-redirects to the apex with the path preserved (`www/ru` to `gidlist.com/ru`), so there is one canonical host and the canonical tags point at it. |
| 22 | **Connect Payme and Click** | Phase 9. The billing structure is built and the seam is lib/billing/provider.ts; this is the provider implementations plus their webhooks, which are the only thing permitted to write a subscription row. Needs the registered entity and signed contracts. **Click application submitted (28 Aug 2026); awaiting their callback.** Onboarding is: register in Click Business with the INN, sign via Didox with an ЭЦП, receive the QR and cash register — then request website/API access **separately**, which is gated on implementing Prepare and Complete rather than on paperwork. Documents needed: director’s passport, Guvohnoma, Uzbek bank account + MFO, and IKPU/VAT/receipt type for fiscalisation (IT Park exemption matters here). Published tariffs: QR 1–2%, website 1.5–2.5% local cards and 2% Visa — SaaS is not a listed sector, so confirm. **Two things to ask them that shape the model:** whether Click settles for a subscription rather than goods, and whether recurring billing exists or each renewal is a fresh payment. **Blocked on a document:** the exact `sign_string` construction is not readable from docs.click.uz, which renders client-side — the merchant integration PDF is needed before the handler can be written, and guessing at a signature algorithm is not an option. Paddle for customers outside Uzbekistan, since Stripe does not onboard Uzbek businesses. |
| 23 | ~~**The app's root page was the Phase 0 placeholder**~~ | **Done.** `/` no longer renders anything — it redirects, signed in to `/dashboard` and signed out to `/login`. Restyling the placeholder would have been the wrong fix: `app.gidlist.com` is the product and `gidlist.com` is where the selling happens, so a landing page here would compete with the marketing site (21) for the same words and drift from it the moment either was edited. Deliberately **not** done in `proxy.ts`, which already resolves the user and reaches the same conclusion for `/login` and `/signup` — that file is the security boundary gating every private route, and a cosmetic routing preference does not justify editing it. Not a permanent redirect: the destination depends on who is asking, so it must never be cached. |
| 24 | ~~**Google sign-in shows Supabase’s domain, not Gidlist**~~ | **Done, 30 Aug 2026 — and the earlier diagnosis in this row was wrong, which is worth recording.** This entry previously said the consent screen named the OAuth redirect host by design and that *"the only fix is Supabase’s custom auth domain add-on"*, paid and monthly. That was incorrect. The real cause was that Google was **not displaying the branding at all** — app name and logo — because brand verification was outstanding, so it fell back to the raw redirect host. The blocking issue was a single line in the verification panel: *"The website of your home page URL https://gidlist.com is not registered to you."* Google requires the Cloud project’s own account to have proved ownership of the home-page domain in **Search Console**; verifying `gidlist.com` there as a Domain property, with a TXT record in Cloudflare, cleared it. The consent screen now reads Gidlist. Cost: nothing. **The lesson, since this cost real time:** "Google shows X by design" was inferred from how OAuth works rather than read off the console, and the console had been saying exactly what was wrong the whole time behind a *View issues* button. **Prerequisites completed along the way:** publishing status moved off *Testing* (until then only listed test users could sign in with Google at all — a functional blocker, not a cosmetic one), and `/[locale]/privacy` and `/[locale]/terms` were built in all three languages because Google will not publish a consent screen without both links. **Still worth knowing:** the authorized domain list contains the `*.supabase.co` host, which cannot be verified because the domain is not ours. Google has not demanded it so far. If a future verification round does, the custom-domain add-on becomes genuinely necessary rather than merely cosmetic. And the User support email shown on the consent screen is still a personal Gmail — see item 37. |
| 25 | ~~**Per-currency pricing (`plan_prices`)**~~ | **Done.** `plan_prices (plan_code, currency, price_minor)`, the shape `addon_prices` already used. USD is seeded from the existing `plans` rows rather than retyped, so the migration cannot introduce a disagreement with what the product charges on the day it runs. **So’m: 0 / 59,250 / 177,750 / 474,000** — a deliberate local price list, **not a conversion**, and it must never be recomputed from an exchange rate or every Uzbek customer is silently repriced when the rate moves. **No VAT** — the company is an IT Park resident, so the figure is what the customer pays. **The minor-unit trap:** `packages/core/src/money.ts` is the authority and gives UZS an exponent of 0 because the tiyin is defunct, so 59,250 so’m is `59250`, not `5925000`; treating it as two decimals would inflate every Uzbek price a hundredfold and look like a pricing decision rather than a bug. **The site now reads prices from the database** (same fetch-with-fallback pattern as the copy), so `apps/site/src/lib/pricing.ts` is only the fallback for an unreachable database — the hand-mirroring drift is gone. Currency by locale is a **named heuristic**: `uz` and `ru` see so’m, `en` sees dollars, so a Russian speaker in Riga sees the wrong one; it only affects a marketing page, because currency is frozen on the subscription at purchase. `formatPrice` writes the so’m name itself (`so‘m` / `сум`) because CLDR has no Russian symbol for UZS and `Intl` falls back to the code, rendering "59 250 UZS" — the number is still formatted by `Intl`. **Interim:** `plans.price_minor` remains as the base for the SQL billing functions and the in-product billing page; migrating those to `plan_prices` is what remains before a customer can be charged in so’m. |
| 26 | ~~**Gateway selection by currency**~~ | **Done.** `activeGateway()` read one `PAYMENT_PROVIDER` and returned one gateway, so a deployment could only serve one market. Replaced by `gatewaysFor(currency)` reading `PAYMENT_PROVIDERS` (comma-separated), so an Uzbek customer paying in som and a German one paying in dollars are served by different providers from the same build. **Returns a list, not one gateway** — for som there genuinely are two, Payme and Click, and the customer has a preference between them; collapsing that here would mean picking on their behalf with no basis to pick. `isCheckoutAvailable()` now takes a currency for the same reason: with two currencies live, the question has no single answer. Still no implementations — that is item 22, and it is now only the provider code plus webhooks. |
| 27 | ~~**No way to edit prices without SQL**~~ | **Done.** Admin → **Plans and pricing**, behind a new `billing` capability kept separate from `accounts` — seeing what customers pay and changing what they will pay are different jobs. **Narrow on purpose:** UPDATE only, so insert and delete stay impossible through the API for everyone; `code` and `is_free` are frozen by a trigger, the first because `subscriptions` references it and editing it would orphan live customers, the second because it decides whether checkout is offered at all. A free plan priced above zero is refused outright. Every change writes `plan.changed` to `audit_log` with before and after values, because a price is exactly the kind of thing somebody may later be asked to justify. The form takes whole currency units and converts with `Math.round(major * 100)`, since `price_minor` is where a missing decimal turns $40 into $4,000 with no visible difference. **The page warns that gidlist.com does not follow it** — the site’s figures are still hand-mirrored in `apps/site/src/lib/pricing.ts` until item 25. |
| 28 | ~~**Photo and file evidence on an item**~~ | **Done, after one restructure.** The first version made `evidence` a single enum — none, photo, file — so photo and file were *alternatives*, and one `evidence_required` flag covered whichever was picked. That was wrong: an item could not ask for a photograph of the fridge **and** a signed delivery note, nor demand one while merely inviting the other. **The shape now is three features × two switches:** `photo_enabled`/`photo_required`, `file_enabled`/`file_required`, `location_enabled`/`location_required`. Nothing implies anything else. `submission_items` has a slot per kind, since both can exist at once. On a phone `capture="environment"` opens the rear camera directly. **The bucket is private — the only one in this schema.** Logos are decoration; a photograph of a shop floor is that customer’s operational data. Display goes through signed URLs minted server-side in one batch call. **Paths are built server-side** as `<board_id>/<submission_id>/<answer_id>-<uuid>.<ext>` — the first segment is what the storage policy authorises on, so a client-chosen path would be a route into another company’s folder; the original filename is discarded apart from its extension. Replacing deletes the old object; a failed row update deletes the object just written. **The interface never says "verified":** a photograph proves something was photographed, not that it was photographed here or now. |
| 29 | ~~**Item type: location**~~ | **Done.** An item can carry `location_lat`, `location_lng` and `location_radius_m` — all three or none, enforced by a check constraint, because two of three is a half-configured rule that either never fires or rejects everybody. **The radius has a 25m floor.** GPS is roughly 5–20m outdoors and far worse indoors, which is exactly where warehouses, kitchens and clinics are; a tighter radius would reject people standing in the right place and read as the product being broken. **The accuracy rule is the feature, not a footnote:** each reading is stored with the accuracy the browser reported, and a tick is refused only when somebody is *certainly* outside — `distance - accuracy > radius`. An imprecise fix gets the benefit of the doubt and the stored accuracy shows a reviewer how much doubt there was. Capture uses `enableHighAccuracy` with `maximumAge: 0`, so a cached position from where the phone was ten minutes ago is refused. Position is read only when ticking, never when un-ticking — asking permission to record where somebody was when they changed their mind would be indefensible. **Still not proof:** browser geolocation is trivially spoofable via devtools or a mock-location app, and the interface never calls it verified. |
| 30 | ~~**Require evidence before an item can be ticked**~~ | **Done.** Each of photo, file and location carries its own `*_enabled` and `*_required` pair, so a photo can be mandatory while a file is merely invited and location is only recorded. **Enforced by a `before update` trigger on `submission_items`, not by the form** — ticking is a plain PostgREST update, so a check in TypeScript is a courtesy a crafted request walks straight past. **Parent items are exempt, per the decision:** a parent completes by rollup, so a requirement on it has no moment at which anybody could satisfy it and would deadlock the trigger doing the completing. The builder does not offer the choice on a parent either. The guard fires only on the transition to ticked, so commenting, attaching and un-ticking are untouched. A requirement cannot be enforced while its feature is switched off — refused by the schema, and the enforcement control is not rendered at all until the feature is on. **Two headers of my own defeated this in ways that looked like device problems:** server actions cap request bodies at 1MB by default, so a phone photo never reached the upload action; and `Permissions-Policy: geolocation=()` made the browser refuse `getCurrentPosition` **without prompting**, so no permission request appeared and the site never showed under Chrome’s Location settings. Both are fixed and both are commented where they live — a blanket denial written when “the app asks for none of these” was true stays correct silently and becomes wrong silently. **Open, and now queued separately as item 36:** an optional submit-by deadline judged on when items were *ticked* rather than when the submission was sent, which is what makes filling in offline and syncing later workable. |
| 31 | ~~**Make the product installable (PWA)**~~ | **Done.** `app/manifest.ts`, icons drawn from the brand mark, an `/offline` page and a service worker. **The worker caches exactly one thing — the offline page.** Nothing else is ever stored, and the fetch handler ignores everything that is not a page navigation: a service-worker cache has no session attached, so anything user-scoped in it is served back to whoever asks next, including a different person on a shared device. Same rule that keeps `unstable_cache` restricted to rows whose SELECT policy is `to public using (true)`. **Two things that had to change to make it work at all.** `worker-src 'self'` in the CSP — `strict-dynamic` makes the browser ignore `'self'` in `script-src`, so worker registration was refused with only a console error. And `manifest.webmanifest` and `sw.js` had to leave the proxy matcher: a browser fetches the manifest **without credentials**, so the proxy saw no session, treated it as a private route and redirected it to `/login` — the browser then had no valid manifest and silently never offered to install. Found by requesting the manifest and reading what came back, which was a login page. `/offline` is in `PUBLIC_ROUTES` and deliberately says nothing it cannot know: the worker caches no checklists, so it promises only that nothing was sent. **Real offline** — filling in a checklist in a basement and syncing later — is a queue of pending writes in IndexedDB, not a response cache, and belongs with Phase 8. |
| 32 | **Create a plan from the admin screen** | The editor is UPDATE-only today, and adding a plan was deliberately excluded because a plan code is not just a price: it needs `plan_features` rows, a `plan_prices` row **per currency**, and a place in the interface. It is worth revisiting once payment providers are connected, because by then a fourth thing is true — the plan must also exist on the provider’s side, and Click, Payme and a Merchant of Record each model that differently. **The real question is whether a plan is data or code.** If the form can create one, it must also create its feature rows and its per-currency prices, or it produces a plan that renders with no capabilities and no price in som. A guided flow that insists on all of it is the only version worth building; a bare "add row" button would be a way to break billing from a web page. Insert and delete have no RLS policy at all today, so this is a database change as well as a screen. |
| 33 | ~~**Group the marketing-copy editor by where the text appears**~~ | **Done.** Nine sections in page order — Search results and sharing, Navigation, Hero, The problem, What it does, How it works, Pricing, Closing call to action, Footer — replacing a flat alphabetical list of 57 dotted keys where changing the hero meant knowing it is called `headline` and `subhead`. The grouping lives in `siteContentSections()` beside the catalogue, not in the screen, so the site declares its own order and the editor renders it. **Sections claim keys by prefix**, so `features.4.title` lands in the right group with nothing to register, and **the last section is a catch-all**: a string matching no prefix appears under "Not yet grouped" rather than vanishing. Silently dropping one would be the worst outcome — copy live on the site and uneditable, with nothing to say why. Within a section, keys sort by which prefix matched rather than alphabetically, so the hero reads tagline, headline, subhead, then buttons; and `.title` sorts above `.body`, because plain alphabetical put every card’s second half first. Search filters inside each section and drops the empty ones, so a query never leaves a heading with nothing under it. Verified by running the function: 57 keys in, 57 grouped, no duplicates, catch-all empty. |
| 34 | ~~**The installed app’s launch screen is ugly**~~ | **Done.** `background_color` is now the brand blue rather than the page background. Android builds the splash from that colour with an icon it picks itself, and against near-white it chose the maskable icon — a hard-edged full-bleed square by design — leaving a sharp blue tile floating on off-white. Setting the field to the same blue means **it no longer matters which icon Android picks**: the maskable one dissolves into the background leaving just the white mark, and the rounded `any` one shows a barely visible edge. Both read as deliberate, and the result stops depending on a heuristic outside our control. The cost is a brief blue field before the app paints its own background, which is the normal shape of a branded launch screen. **Still open for iOS:** it ignores the manifest here and wants `apple-touch-startup-image`, which needs one asset per device size; without it an installed app shows a white screen briefly. |
| 35 | ~~**A submission did not record who filled it in**~~ | **Done, in two parts.** **(1) Who did it:** `submissions.submitted_by` and `submitted_by_email`, written only by `submit_submission()` from `auth.uid()` inside a SECURITY DEFINER function — never from an argument, because a client-supplied name on the row that proves who did the work would be a signature anybody could forge. Compliance gained a **Filled by** column *beside* the assignee, not instead of it: "who was asked" and "who did it" are different questions, and answering the second with the first is what produced "Anyone". **Not back-filled** — nobody recorded it at the time and a plausible guess would be invented evidence; those rows show an em dash. **(2) The rule:** `schedules.assignment_mode` is `creator`, `everyone` or `specific`, never inferred from an absence. **`everyone` means every active member, one obligation each** — the membership list expanded, following who joins and leaves — not a single record anybody may claim. After this no new submission has a null assignee. `specific` requires at least one name, enforced by a **deferred** constraint trigger, because a schedule is written before the names it is about to be given; the same trigger on delete stops a schedule being emptied while claiming to name people, and the mode buttons on each schedule are the way out of that. Naming somebody is what switches a schedule to `specific` — the create form offers only the two modes valid before assignees exist, so there is no state where the interface says "specific people" and means nobody. **Backfill changes behaviour:** schedules that named nobody become `everyone`, so a space of five now generates five obligations per occurrence where it generated one. Stale unassigned `upcoming` rows for future dates are deleted so they do not sit alongside the new per-member ones; drafts, done and missed are untouched and keep reading "Anyone", which is what was true when they were made. |
| 36 | **Optional submit-by deadline, judged on tick time** | Raised while answering item 30: a task may optionally carry a time by which it must be done, and **the deadline is measured against when each item was ticked, not when the submission was sent**. That distinction is the whole point — it lets somebody tick items on time in a basement and submit when signal returns, without the delay counting against them. `submission_items.checked_at` already records the per-item time, so the data is there. **Needs deciding:** where the deadline lives (item, checklist or schedule), what a late submission is recorded *as* — late is not the same as missed, and neither is the same as on-time — and whether compliance counts it separately, which is the same reasoning that made voiding its own status rather than a deletion. **Note the offline half is not real yet:** ticking today requires a network call, so "ticked on time, submitted later" only becomes meaningful once there is a queue of pending writes in IndexedDB, which belongs with item 31 and Phase 8. |
| 37 | **Move the project off a personal Google account** — *deferred until after market testing (29 Aug 2026)* | Every external service Gidlist depends on — Supabase, Vercel, GitHub, Resend, Cloudflare, the registrar, the Google Cloud project holding the OAuth client — is owned by one personal Gmail account. Losing that account loses the product, and there is no support queue that restores it. **Use an address on `gidlist.com`, not a new `@gmail.com`:** the Google consent screen shows the support address to every person who signs in (see item 24), Google can suspend a gmail.com address with no appeal, and Click/Payme check contact details against the company. A Google account can be created against a non-Gmail address, so this does not require Workspace — Cloudflare Email Routing into an existing inbox is enough. **Three tiers of work:** config values (`ALERT_EMAIL`, `EMAIL_FROM`, merchant contact details) are minutes; Supabase, Vercel, GitHub, Resend, Cloudflare, Search Console and the registrar are invite-then-remove; the Google Cloud project needs the new account added as Owner rather than the OAuth client recreated, because recreating it means new credentials in Supabase Auth and a window with Google sign-in broken. The owner account *inside* Gidlist is a database row — invite the new address and grant it owner through the app. **Two constraints.** Keep the personal account as a second owner on Supabase and Vercel permanently; it is the only way back to the production database if the project account is locked out. And leave the Click/Payme contact email alone until that application is approved — it went in 28 Aug 2026 and changing the contact mid-review stalls it. |
| 38 | **Archive or delete a checklist** — *requested 30 Aug 2026* | There is no way to retire a checklist today; a template written once is on the board forever. **The decision this needs is what "delete" means, and the product already has an answer for the neighbouring case:** submissions are never deleted, because an operational record that can vanish is not a record. A checklist is not a submission — it is a template — but it is the *parent* of submissions, so deleting one either orphans the history or destroys it, and neither is acceptable silently. **Likely shape:** archive is the primary action — the checklist stops being schedulable and leaves the board, its past submissions stay readable, and it can be restored. Hard delete is then only offered for a checklist with **no** submissions at all, where nothing is at stake; past that point archive is the only option, and the interface should say why rather than greying out a button. Touches `checklists`, the schedules that reference it, RLS for the new state, and the board listing. |
| 39 | **Grant every capability from the access screen** — *requested 30 Aug 2026* | Asked for as "unlimited access". **There are two readings and they are very different.** The harmless one is a single control granting all five existing capabilities — `translations`, `site`, `accounts`, `billing`, `grants` — at once. That is only a convenience: the same result is already reachable by ticking five boxes, so it adds no power that the screen does not already hand out. Worth building. **The other reading is `root`, and that must not be added.** `root` is deliberately read-only on this screen and settable only by SQL, and the comment in `access/page.tsx` states the reason: it is what stops this page from being a route to full control. Anyone holding `grants` can already grant `grants`, so if the screen could also grant `root`, any grants-holder could make themselves root in two clicks and there would be no capability left that the interface cannot confer on itself. The SQL-only requirement is the whole control — it means escalation needs database access, not a session. **Decision needed** before building: all five, or root as well with that consequence understood. |
| 40 | ~~**gidlist.com as an award-standard site**~~ | **Done, live.** Rebuilt as a designed experience: fourteen scenes and ten interactive modules — the enforcement sandbox, the schedule builder, the hierarchy explorer, the insights chart — driven by GSAP and IntersectionObserver, with `prefers-reduced-motion` honoured throughout. **three.js was deliberately not used.** It is well over 100 KB gzipped before a scene exists, the audience is largely on mobile data, and nothing on the page needed a third dimension to make its argument; the weight would have cost more in performance scoring than the effect could win back. Copy stays CMS-editable and no animation is pinned to a string length. **Constraint (1) as originally written was wrong, and finding out was expensive.** The site set a CSP with a per-request nonce and `strict-dynamic`, which cannot work on statically generated pages: a prerendered page carries whatever nonce it was built with, the response header carries a fresh one, they never match, and the browser refuses every script. The site had shipped this for months with nothing interactive on it, so the fault was invisible until the rebuild needed JavaScript and the whole page went inert in production. See 41 for what replaced it. |
| 41 | ~~**Where the functions run**~~ — *30 Aug 2026; measured, and the first answer was wrong* | **The rule: put the function nearest whoever is waiting, and next to the data only when nobody is.** The product is pinned to `fra1` (Frankfurt) and the marketing site to `dub1` (Dublin). Supabase production is `eu-west-1`, Dublin. **How it went wrong first.** Neither project was pinned; Vercel had put the product in `fra1` and the site in `iad1` (Washington). Reasoning that a query from Frankfurt to Dublin costs ~20–25 ms and that `proxy.ts` runs on the Node runtime — so its `getUser()` pays that on *every* request — both were pinned to `dub1` to sit beside the database. The prediction was that any request making one or more round trips comes out ahead, on the assumption the extra user→Dublin distance cost ~10 ms. **The measurement said otherwise.** From Andijan, `app.gidlist.com/login` went from a median of 884 ms (min 677) on `fra1` to 1003 ms (min 736) on `dub1`, warm, ten samples with four discarded warm-up hits. The minimum is the trustworthy figure since it carries the least noise: **+59 ms**, not −20. Dublin is roughly six times farther from Uzbekistan than the estimate allowed, and the login page makes exactly one database round trip — so the saving never covers the distance. **Why the site keeps `dub1` anyway, and it is not inconsistency.** Its pages are statically generated and CDN-cached; a function only runs during ISR revalidation, which is server-to-server with no user waiting. There the only latency that exists is function→database, so sitting in Dublin is strictly better and costs nobody anything. **What would change the answer for the product:** a signed-in page making several sequential queries pays the round trip several times, and could tip back toward `dub1`. That needs measuring on a real authenticated page rather than the login screen, which is the lightest page in the app and the only one measurable without credentials. **Note:** `vercel.json` cannot carry comments, which is why this lives here. Verify after a deploy by reading `x-vercel-id` — its format is `edge::function`. |
| 42 | **The site trades `unsafe-inline` for scripts** — *reviewed 31 Aug 2026, accepted* | The marketing site sends `script-src 'self' 'unsafe-inline'`, weaker than the product's nonce policy. **Hashes were considered and rejected as unworkable here:** each page carries six inline blocks, ~35 KB, of Next's RSC payload, and that payload changes whenever the CMS edits a string because the site regenerates through ISR. A hash list would go stale on a content edit and blank the page — reintroducing exactly the failure in 40, but triggered by an administrator changing a headline rather than by a deploy. **What limits the damage:** the site has no session, no user data and no browser-side database access, and `connect-src` is `'self'` alone, so an injected script has nothing to read and nowhere to send it; `object-src 'none'`, `base-uri 'self'`, `form-action 'self'` and `frame-ancestors 'none'` all remain. The residual risk is defacement of a public page, not disclosure. Revisit if the site ever grows a form that collects anything. |
| 43 | **Security pass** — *done 31 Aug 2026; two items remain, neither a code change* | Audited rather than assumed, and it came back largely clean. **Verified:** RLS enabled on all 28 tables, 82 policies, and every `using (true)` is a `select` on a catalogue table — plans, locales, translations, marketing copy — which is public by design. All 120 `security definer` functions set `search_path`, no exceptions. `submission-evidence` is the one **private** bucket, correctly; only banners and avatars are public. The service-role key appears solely in `lib/supabase/admin.ts` behind `server-only`, never `NEXT_PUBLIC_*`. The `next` redirect parameter is guarded in both the sign-in action and the OAuth callback — the backslash bypass was tested against the WHATWG parser and does not escape the origin. The single `dangerouslySetInnerHTML` is JSON-LD with `<` escaped. HSTS, nosniff, Referrer-Policy, Permissions-Policy and X-Frame-Options are set on both apps. **Rate limiting is enforced in the database, not the application** — triggers on invitations, a wrapper on `materialise_schedule` — which is why it does not appear in app code, and also why it holds against direct PostgREST calls. **Fixed:** `nanoid <3.3.18` (GHSA-2v37-7h3g-55p8, high) reached the tree through `@tailwindcss/postcss` and `postcss`; pinned by a pnpm override. Build-time only, so it never served a request, but the audit now reads clean — which is what keeps an audit worth reading. **Still open:** (b) Item 37. **(a) is closed — see 50.** |
| 50 | ~~**Storage objects were backed up by nothing**~~ — *was 43(a); done 3 Sep 2026* | Supabase's backups cover the database and not storage. The evidence photographs are what a customer would actually produce as proof, and the reason they pay, and no copy of them existed anywhere. Now mirrored nightly to **Cloudflare R2** by `/api/cron/storage-backup` at 04:00 UTC, an hour after the cleanup drain so a file about to expire is not copied first. **R2 rather than S3 because egress is free — a backup you are billed to read is one you hesitate to test, which makes it not a backup.** `aws4fetch` rather than the AWS SDK: signing is the only thing needed and it is ~5 KB against megabytes. **A ledger, not a listing.** The obvious design lists the destination each night and copies the difference, which means paginating S3 XML and holding every key in memory — growing with the archive, doing the most work on the night it is largest. Instead `storage_backup_log` records what was sent, and "what is left" is an indexed anti-join that arrives already limited to one batch. The honest cost: it records what we *sent*, not what R2 *holds*, so an object lost at the far end would go unnoticed. That gap is real, is not closed, and wants a periodic verification pass that reads the destination back. **The ledger is written after the upload, never before** — the other order marks a file safe that no destination received, and the anti-join then never offers it again. **Deletion is the subtle half.** A backup that deletes what the source deleted reproduces the accident you wanted protection from, so this one deletes almost nothing. The exception is retention, and it is not really an exception: the product *sells* `evidence_retention_days`, so an off-site copy that outlived it would turn a promise about deletion into a lie, in a product about compliance. `storage_cleanup_queue` already separates the cases perfectly — a row exists there only because `expire_evidence()` put it there deliberately, so an accidental deletion leaves no row and never propagates. `maxDuration` is 60s, the ceiling on every plan including Hobby; asking for 300 would fail the build on Hobby, so the batch is 20 copies per run and a backlog drains over several nights. **Registered in `job_expectations` in the same migration**, because 15b found `expire-evidence` had never been registered and an empty alert list reads as good news. To drain a backlog faster, call the endpoint by hand with the same Bearer secret — it is idempotent. |
| 44 | **Mobile apps and Google Play** — *queued 31 Aug 2026; not ready to start* | Asked for with "if we're ready for that", and the honest answer is not yet. **What is missing is not effort, it is the thing that makes a native app worth having.** The product is already installable as a PWA (31) and works on a phone browser, so a wrapper around the same screens would add a store listing and almost nothing else. The feature that only a real app can deliver is **filling a checklist with no signal** — a queue of pending writes in IndexedDB that syncs later, which 31 explicitly does not do and 36 depends on. Building the store presence first would mean shipping, to a public store with reviews, a version whose one advantage over the web is absent. **Prerequisites, in order:** (a) the offline write queue, shared by web and native so the rule about when a tick counts lives in one place; (b) an Expo app in `apps/mobile` consuming `packages/core`, which is why that package has no React or Next imports; (c) the store work. **The store work needs things only the account owner can do:** a Google Play developer account (a one-off 25 USD, and identity verification that can take days), an upload signing key, the Data Safety declaration — which must describe the photo, file and *location* capture truthfully — and a privacy policy URL, which exists at gidlist.com/privacy. **Also true and worth writing down:** per item 1, the product has still never been used on a phone by anybody. That should happen before it is submitted to a store, not after. |
| 45 | **Member hierarchy, phase B — visibility** — *phase A shipped 31 Aug 2026* | Phase A is done and deliberately inert: `board_members.manager_id` records reporting lines within a space, a trigger refuses self-reference, cross-space managers and loops, and `board_member_reports()` / `is_manager_of()` walk the chart. **Nothing reads any of it yet, on purpose.** Item 11 said design before building, and the reason stands: visibility today has two cases — a member sees themselves, an editor sees everything — and a hierarchy adds a third, "mine and my reports'", which every submission and compliance policy has to express. Doing that in the same change that introduced the data would have meant altering who can read compliance history with no way to test the model apart from the rules built on it. **Phase B: the three questions are answered and the database half is built (4 Sep 2026); the interface half is not.** **(1) See and void, not tick or submit** — voiding is a judgement about whether a record should count, which is a supervisor's job; doing the work is not. **(2) Yes, it reaches compliance and the evidence** — a supervisor given the records but denied the figures drawn from them has the raw material and not the answer. **(3) Visibility follows the *current* chart**, so a manager change hands the history to the new manager and takes it from the old one. That third answer has a consequence worth stating: somebody who reviewed and voided records in March cannot see them in April if that person now reports elsewhere. What survives is **accountability rather than access** — `submissions.voided_by` and `audit_log` both keep who decided what, permanently. The alternative, keeping access to the period you were responsible for, means storing when each reporting line began and ended and answering every visibility question against a date range instead of a fact: a much larger model, bought for a case nobody has asked for. **Built in `20260904090000_hierarchy_visibility.sql`:** `manages_member(board_id, user_id)` bridges the two id spaces phase A left unjoined — `is_manager_of` speaks in membership ids while `assignee_id` is a user id — and both memberships must be `active`, or an unaccepted invitation would become a way to read a colleague's records. It is an *ancestor* test, not a parent test: a head of department who cannot see past their own supervisors describes an organisation nobody runs. `submissions_select` and `submission_items_select` gain one clause each, with the existing three reproduced exactly, since a policy is replaced whole and quietly dropping a case removes access that works today. `set_submission_void` widens rather than replaces, so an admin still governs everything. Compliance needed no change — those functions are `stable` and not `security definer`, so RLS applies inside them and the new clause simply flows through. Storage needed none either: evidence read was already board-membership-wide. **Eleven checks added to `supabase/tests/security.sql`**, including the two that matter most — a manager cannot see a *non*-report's record, and access disappears when the reporting line is removed. **Still to do:** the compliance page still says "You are seeing your own submissions", which is now a lie to every supervisor; the void control is shown on `canGovern` alone, so a manager cannot yet reach it; and the thing actually asked for alongside this — reviewing a record with photographs and files attached is awkward, and wants a proper review view. |
| 46 | ~~**Opening a space landed on the builder's list**~~ | **Done, 3 Sep 2026.** The space root *was* the checklist library, so everybody — including the members who only tick things off, who are most of a space — arrived at a page of templates. The library moved to `/checklists` and the root now redirects to `/fill`. **A redirect rather than moving Fill in up to the root**, so the two tabs keep separate addresses: the tab bar can then mark exactly one as current, and a link to the library reads as a link to the library rather than to the space. It must not be a permanent redirect — `/checklists` sends members *back*, so a cached 308 would strand somebody in a loop the moment their role changed. Tabs also became current for their whole subtree, since exact matching left no tab marked on a submission or a checklist's own pages. |
| 47 | ~~**Interface strings that were English in every language**~~ | **Done, 3 Sep 2026.** Found by reading the space in Uzbek rather than by grep: "Back to checklists", the Spaces breadcrumb, the logo and banner section copy, the Remove button on a member, the banner preset names, every upload hint and every upload error. **The two structural causes, which matter more than the list.** `MEDIA_LIMITS` in `packages/core` carried a `hint` *sentence* — an English string in a shared constants file renders as English wherever it is displayed, so it now carries a `hintKey` and the catalogue owns the wording. And `validateMediaFile` returned a finished English message; it now returns `{ key, values }`, so the one place a person is told why their photograph was refused answers in their own language. Nothing on the server calls it, which is what made that safe. `ImageUploadForm`'s `label` prop lost its `'Upload'` default: a default in the source language means a call site that forgets the prop ships untranslated and nothing says so, where a required prop makes it a type error. Verified by asserting all 21 new keys plus all 12 preset keys resolve in **all three** catalogues, not just English. |
| 48 | ~~**Compliance table had no per-column filters**~~ | **Done, 3 Sep 2026.** A filter row under the header for Checklist, Assignee, Filled by and Status. **They write to the same URL parameters as the panel above** — `useComplianceFilters`, shared by the panel, the columns and the pager — so the two surfaces are one control and cannot disagree, and any view is shareable by link. Date deliberately has no column control: a range is two values with a labelled From/To directly above, and a third place to set it would be a third thing to keep in agreement for no new capability. **Filled by needed a new function**, `compliance_submitters`, because who was *asked* and who *did it* are different questions and the assignee list cannot express the case the column exists to show; it is not `security definer`, matching `compliance_assignees`, so RLS decides what each viewer's dropdown offers — a member's lists only themselves. It carries a **`NONE` sentinel** for "not filled in", which is every missed and upcoming record and cannot be an empty string, since an empty parameter is indistinguishable from no filter. **Two bugs found while building it.** Changing a filter kept the page number, so narrowing from page 4 showed an empty table reading "No submissions match these filters" — a lie about the filters rather than a fact about the data; the shared hook now resets `page` on every change except paging itself. And the empty state replaced the whole table, taking the filters with it, so the only way to widen a search that matched nothing was the browser's Back button. A missing `compliance_submitters` is swallowed rather than thrown, so the report still renders on a database that has not had the migration yet. |
| 49 | ~~**The fill page's date picker was still native**~~ | **Done, 3 Sep 2026.** `<input type="date">` draws a calendar the *browser* labels, so somebody running the product in Uzbek on an English-language phone got an English calendar with no API to change it — which is exactly why `DateField` exists, and this was the one screen that had never adopted it, so the fill page and the scheduling screen disagreed about what language the product is in. Now `DateField`, keeping the day-at-a-time arrows either side, which are the common gesture on a phone. **`DateField` gained a `today` prop in the process, and that is the part worth keeping.** It computed "today" from the browser's clock; the fill page resolves it on the server in the *space's* timezone, because a night shift working at 01:00 in Tashkent is still on the previous day's list while the browser's calendar has moved on. The prop is optional and falls back to the browser, which is right for the schedule form where the person filling it in is the only frame of reference. Both the cursor's first month and the Today button read it, since the two disagreeing would be a bug waiting for somebody to move the reset. The `.native-date-plain` rule in `globals.css` went with it — no input left to hide an indicator on. |
| 49x | ~~**Uzbek apostrophes — not a code problem**~~ | *Investigated 3 Sep 2026 and closed without a change.* The suspicion was that `roʻyxatlari` was shipping as `ro'yxatlari`. A first grep appeared to find 71 offenders in `uz.ts`; it was matching **TypeScript syntax** — a key like `windowTo'` is an `o` followed by the closing quote — and the real test, an apostrophe with a letter on both sides, returns **zero** across `uz.ts`, `ru.ts`, `en.ts` and `site-messages.ts`. Every `oʻ`/`gʻ` in the source already uses U+02BB. So any wrong character is in the **database overrides**, which beat the catalogue for every string — see the note on Admin → Translations — and can only be fixed there. A query that finds them is in the handover notes. |belled by the *browser*, so it ignores the app's language — the same fault fixed on the compliance filters and in scheduling. Left alone because it is wrapped in prev/next arrows and a Today button, and dropping `DateField` in changes the layout of the one control a filler uses every day; worth doing deliberately rather than as part of a translation sweep. |

## Build order

| Phase | Status | Delivers                                              |
| ----- | ------ | ----------------------------------------------------- |
| 0     | ✅     | Monorepo, database, auth, protected shell             |
| 1     | ✅     | Boards: branding, members, ownership                  |
| 2     | ✅     | Checklist builder: groups, 5-level drag-drop, versions |
| 3     | ✅     | Schedules: recurring + specific dates, assignees      |
| 4     | ✅     | Fill mode: drafts, comments, parent auto-complete     |
| 5     | ✅     | Cases dashboard, charts, filters                      |
| 6     | ✅     | Multi-language + in-app translation editor            |
| 7     |        | Plans, limits, paywall (no live provider yet)         |
| 8     |        | Mobile apps (Expo), reusing phases 1–7                |
| 9     |        | Payme / Click / Paddle — once the entity is registered |
